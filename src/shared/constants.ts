import { PlayerData } from "./types/PlayerData";

export const DEFAULT_PLAYER_DATA: PlayerData = {
    taps: 100,
    gems: 100,
    settings: {
        music: true,
        sound_effects: true
    },
}